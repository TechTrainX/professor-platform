import { ArrowUpRight, Clock3, Gauge } from "lucide-react";
import { Link } from "react-router-dom";
import { useTools } from "../../hooks/use-platform-data";

export function TeachingToolsPage() { const { data: tools, isLoading } = useTools(); return <div className="lab-page"><section className="lab-intro"><p className="eyebrow">Interactive systems & learning lab</p><h1>Tools for the moment<br /><em>the idea clicks.</em></h1><p>Browser-based instruments for explaining control, signals, energy, and edge intelligence in a live classroom.</p><div className="lab-meta"><span><b>09</b> instruments</span><span><b>HTML</b> / CSS / JS</span><span><b>FULLSCREEN</b> ready</span></div></section><div className="tool-library-head"><span>THE LIBRARY / {tools?.length?.toString().padStart(2, "0") ?? "--"} PUBLISHED</span><span>SELECT A TOOL TO BEGIN</span></div>{isLoading ? <p>Loading instruments...</p> : <div className="lab-grid">{tools?.map((tool, index) => <Link className={`lab-card ${tool.color}`} to={`/teaching-tools/${tool.slug}`} key={tool.id}><div className="lab-card-top"><span>0{index + 1}</span><span>{tool.category}</span></div><div className="tool-graphic"><span className="graphic-ring ring-one" /><span className="graphic-ring ring-two" /><span className="graphic-pulse" /></div><h2>{tool.title}</h2><p>{tool.summary}</p><div className="lab-card-foot"><span><Gauge size={14} />{tool.level}</span><span><Clock3 size={14} />{tool.time}</span><ArrowUpRight size={17} /></div></Link>)}</div>}</div>; }
