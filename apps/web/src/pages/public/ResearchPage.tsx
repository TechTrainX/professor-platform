import { ArrowUpRight } from "lucide-react";
import { SectionHeader } from "../../components/content/SectionHeader";
import { useSite } from "../../hooks/use-platform-data";
export function ResearchPage() { const { data: site } = useSite(); return <div className="page-shell"><section className="page-intro"><p className="eyebrow">Research areas</p><h1>Useful systems begin with precise questions.</h1><p className="large-copy">Three connected lines of work, united by a preference for systems that can be measured, explained, and used.</p></section><div className="research-list">{site?.researchAreas.map(area => <article className={`research-row ${area.tone}`} key={area.id}><span className="research-number">{area.number}</span><div><h2>{area.title}</h2><p>{area.summary}</p><span>{area.detail}</span></div><ArrowUpRight size={20} /></article>)}</div><section className="quote-band"><span className="eyebrow">Working principle</span><blockquote>“If a student cannot change it, observe it, and argue with it, it is not yet a good teaching instrument.”</blockquote></section></div>; }
