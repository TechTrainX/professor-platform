import { ArrowLeft, Expand, Info, RotateCcw } from "lucide-react";
import { Link, useParams } from "react-router-dom";
import { useTools } from "../../hooks/use-platform-data";
export function TeachingToolPage() { const { slug } = useParams(); const { data: tools } = useTools(); const tool = tools?.find(item => item.slug === slug); return <div className="tool-detail-page"><Link className="lab-back" to="/teaching-tools"><ArrowLeft size={16} /> Back to the library</Link><div className="tool-detail-head"><div><p className="eyebrow">{tool?.category ?? "Interactive instrument"}</p><h1>{tool?.title ?? slug?.split("-").join(" ")}</h1><p>{tool?.objective ?? "Explore the relationship between a system input and its measurable response."}</p></div><button className="tool-action"><Expand size={16} /> Fullscreen</button></div><div className="simulator-shell"><div className="simulator-topbar"><span><span className="live-dot" /> SIMULATOR / LIVE</span><span>V 1.0.4</span></div><div className="simulator-body"><div className="simulator-graph"><div className="graph-axis x" /><div className="graph-axis y" /><div className="graph-wave" /><span className="graph-label y-label">OUTPUT</span><span className="graph-label x-label">TIME →</span></div><aside className="simulator-controls"><p className="control-heading">CONTROL PANEL</p><label>Duty cycle <output>65%</output><input type="range" defaultValue="65" /></label><label>Load <output>42%</output><input type="range" defaultValue="42" /></label><label>Response time <output>1.8s</output><input type="range" defaultValue="36" /></label><button className="reset-button"><RotateCcw size={14} /> Reset model</button></aside></div><div className="simulator-status"><span><i className="status-square cyan" /> Target response</span><span><i className="status-square orange" /> Measured response</span><span className="status-reading">STABLE / 0.847</span></div></div><section className="tool-instructions"><div><p className="eyebrow">How to use this tool</p><h2>Change one variable. Explain the response.</h2></div><div><p>Start with the duty cycle at 50%. Increase the load and observe how the system moves away from the target. Then tune the response time until the measured line settles.</p><p className="note"><Info size={16} /> This is a classroom instrument. The model is intentionally simplified so the relationship stays visible.</p></div></section></div>; }
