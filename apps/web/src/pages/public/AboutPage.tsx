import { ArrowUpRight } from "lucide-react";
import { Link } from "react-router-dom";
import { SectionHeader } from "../../components/content/SectionHeader";
import { useSite } from "../../hooks/use-platform-data";
export function AboutPage() { const { data: site } = useSite(); return <div className="page-shell"><section className="page-intro"><p className="eyebrow">About the professor</p><h1>Making complex systems easier to enter.</h1><p className="large-copy">{site?.profile.longBio}</p></section><section className="about-grid"><div><SectionHeader eyebrow="A short version" title="The work is technical. The goal is human." /><p className="body-copy">{site?.profile.bio}</p><p className="body-copy">My group builds small, inspectable systems because a working prototype is often the fastest route to a better question. In the classroom, that means students can change a parameter, watch a response, and explain why it happened.</p><Link className="text-link" to="/contact">Get in touch <ArrowUpRight size={15} /></Link></div><div className="timeline">{site?.timeline.map(item => <div className="timeline-item" key={item.year}><span>{item.year}</span><div><h3>{item.title}</h3><p>{item.detail}</p></div></div>)}</div></section></div>; }
