import { ArrowUpRight, Mail, MapPin } from "lucide-react";
import { useSite } from "../../hooks/use-platform-data";
export function ContactPage() { const { data: site } = useSite(); return <div className="page-shell contact-page"><section className="page-intro"><p className="eyebrow">Contact</p><h1>Bring a good question.</h1><p className="large-copy">{site?.profile.availability}</p></section><div className="contact-grid"><div className="contact-panel"><span className="eyebrow">Best for</span><ul><li>Research collaborations</li><li>Doctoral supervision</li><li>Guest lectures and workshops</li><li>Engineering education projects</li></ul></div><div className="contact-details"><a href={`mailto:${site?.profile.email}`}><Mail size={19} /><span>Email me<small>{site?.profile.email}</small></span><ArrowUpRight size={17} /></a><div><MapPin size={19} /><span>Based in<small>{site?.profile.location}</small></span></div></div></div></div>; }
